import { integratedRepositoriesPlugin } from './plugin';

describe('integrated-repositories', () => {
  it('should export plugin', () => {
    expect(integratedRepositoriesPlugin).toBeDefined();
  });
});
