node_modules
dist
dist-types
build
coverage

# Docs: properdocs output and the venv mise creates to run it
site
.venv

# Yarn 4: keep the release and plugins committed, ignore the rest
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
.pnp.*

# Local dev app: the 83MB Go template engine is built, not committed
packages/app/public/gotemplate.wasm
app-config.local.yaml
*.sqlite
